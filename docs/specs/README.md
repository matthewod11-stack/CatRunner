# Active Specs

Use this directory for the current design/spec documents that are actively shaping implementation.

- Keep active specs here while they are still source-of-truth for ongoing work.
- Move dated or superseded specs into `docs/archive/specs/`.
- It is normal for this directory to contain only this index between initiatives.
